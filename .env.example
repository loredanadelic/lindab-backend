PORT=3000
NODE_ENV=development

# PostgreSQL (use one of the two options)
# Option 1: connection string
DATABASE_URL=postgresql://user:password@localhost:5432/lindab
# Option 2: separate vars
# PGHOST=localhost
# PGPORT=5432
# PGUSER=user
# PGPASSWORD=password
# PGDATABASE=lindab
